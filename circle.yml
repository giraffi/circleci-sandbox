---
dependencies:
  pre:
    - ci/01_eb_command.sh
  cache_directories:
    - ~/awstools
test:
    override:
        - hoge=`date` && echo ${hoge}
        - date > /tmp/date.txt
        - echo ${hoge}
        - echo `cat /tmp/date.txt`

