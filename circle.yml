---
version: 2
executorType: docker
containerInfo:
  - image: python:2.7.12
stages:
  build:
    workDir: /home/ubuntu/canary-python
    environment:
      - FOO: "bar"
    steps:
      - type: checkout
      - type: shell
        command: echo "When I say Foo... You say $FOO"
