---
test:
  override:
    - bash -c 'until [ -f ./fin ] ; do echo "." ; sleep 2 ; done &'
    - sleep 10
    - touch fin
deployment:
  all:
    branch: /*/
    tags: /*/
    commands:
      - ci/99_sync_bitbucket.sh

